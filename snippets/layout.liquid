{% style %}
  :root  {
    --max-container-width: {{ settings.container_width }}px;
    --section-vertical-padding: {{ settings.section_vertical_padding }}px;

    /* Logo Styles */
    --logo-width: {{ settings.logo_width }}px;

    /* Button Styles */
    --button-radius: {{ settings.button_radius }}px;
  }
{% endstyle %}